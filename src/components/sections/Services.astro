---
import Container from "@/components/ui/Container.astro";
import SectionTitle from "@/components/ui/SectionTitle.astro";
import Card from "@/components/ui/Card.astro";
import { site } from "@/lib/content";
---

<section id="servicios" class="bg-white py-14 sm:py-20">
  <Container>
    <SectionTitle
      eyebrow={site.valueProp.title}
      title={site.services.title}
      description={site.meta.description}
    />

    <div class="mt-10 grid gap-6 lg:grid-cols-2">
      <Card class="bg-surface-muted" padding="lg">
        <h3
          class="font-serif text-h3 font-medium text-slate-900 md:text-h3-desktop"
        >
          {site.services.generalMedicine.title}
        </h3>
        <ul class="mt-6 space-y-3">
          {
            site.services.generalMedicine.items.map((item) => (
              <li class="flex items-start gap-3 border-b border-slate-200/80 pb-3 last:border-b-0 last:pb-0">
                <span class="mt-2 size-1.5 rounded-full bg-primary-700" />
                <span class="text-body text-slate-600">{item}</span>
              </li>
            ))
          }
        </ul>
        {
          site.services.generalMedicine.note && (
            <p class="mt-6 rounded-card border border-accent-100 bg-white px-4 py-3 text-secondary text-slate-500">
              {site.services.generalMedicine.note}
            </p>
          )
        }
      </Card>

      <Card class="bg-surface-muted" padding="lg">
        <h3
          class="font-serif text-h3 font-medium text-slate-900 md:text-h3-desktop"
        >
          {site.services.ultrasound.title}
        </h3>

        <div class="mt-6 hidden space-y-4 md:block">
          {
            site.services.ultrasound.categories.map((category) => (
              <section class="rounded-card border border-slate-200 bg-white p-4 shadow-card">
                <h4 class="text-secondary font-semibold text-slate-700">
                  {category.title}
                </h4>
                <ul class="mt-3 grid gap-x-6 gap-y-2 sm:grid-cols-2">
                  {category.items.map((item) => (
                    <li class="text-secondary text-slate-500">{item}</li>
                  ))}
                </ul>
              </section>
            ))
          }
        </div>

        <div class="mt-6 space-y-3 md:hidden">
          {
            site.services.ultrasound.categories.map((category) => (
              <details class="group rounded-card border border-slate-200 bg-white shadow-card">
                <summary class="flex min-h-11 cursor-pointer list-none items-center justify-between gap-3 px-4 py-3 text-secondary font-semibold text-slate-700 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary-700">
                  <span>{category.title}</span>
                  <span
                    class="text-primary-700 transition group-open:rotate-45"
                    aria-hidden="true"
                  >
                    +
                  </span>
                </summary>
                <ul class="grid gap-2 border-t border-slate-100 px-4 py-3">
                  {category.items.map((item) => (
                    <li class="text-secondary text-slate-500">{item}</li>
                  ))}
                </ul>
              </details>
            ))
          }
        </div>
      </Card>
    </div>
  </Container>
</section>
