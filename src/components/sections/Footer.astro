---
import { Image } from "astro:assets";
import logo from "@/assets/images/logo-transparente.png";
import Container from "@/components/ui/Container.astro";
import { site } from "@/lib/content";
import { withBase } from "@/lib/urls";
---

<footer class="border-t border-slate-200 bg-white">
  <Container class="py-10">
    <div class="grid gap-8 md:grid-cols-3">
      <div>
        <a
          href={withBase("/")}
          class="inline-flex items-center gap-3 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary-700"
          aria-label={site.brand.clinicName}
        >
          <Image
            src={logo}
            alt={site.brand.clinicName}
            widths={[56, 72]}
            class="size-14 object-contain"
          />
          <div>
            <p class="text-sm font-semibold text-text">
              {site.brand.clinicName}
            </p>
            <p class="text-xs text-muted-600">{site.brand.lockupText}</p>
          </div>
        </a>
        {
          site.footer.note && (
            <p class="mt-4 max-w-sm text-sm text-muted-600">
              {site.footer.note}
            </p>
          )
        }
      </div>

      <div class="space-y-2 text-sm text-muted-700">
        <a
          href={`tel:${site.contact.phone}`}
          data-track-event="click_call"
          data-track-source="footer"
          class="block w-fit rounded-card text-muted-700 hover:text-primary-700 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary-700"
        >
          Tel: {site.contact.phone}
        </a>
        <a
          href={site.ctas.primaryHref}
          data-track-event="click_whatsapp"
          data-track-source="footer"
          class="block w-fit rounded-card text-green-700 hover:text-green-800 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-green-700"
        >
          WhatsApp: {site.contact.whatsapp}
        </a>
        <p>
          {site.contact.city}, {site.contact.state}
        </p>
        <p>
          {site.hours.daysLabel} · {site.hours.timeLabel}
        </p>
      </div>

      <nav class="flex flex-col gap-2 text-sm" aria-label="Enlaces del footer">
        {
          site.footer.quickLinks.map((link) => (
            <a
              href={withBase(link.href)}
              class="w-fit rounded-card text-muted-700 hover:text-primary-700 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary-700"
            >
              {link.label}
            </a>
          ))
        }
      </nav>
    </div>
  </Container>
</footer>
